# 华科储能智能会议助手 — AI Coding 协同开发全过程记录

> **用途**：用于比赛展示、PPT 制作、答辩讲解、新对话上下文恢复。  
> **记录范围**：从项目选题、产品定义、技术方案、代码生成、调试踩坑、架构调整到后续规划的完整过程。  
> **核心主题**：利用 AI Coding 能力，将一个模糊业务想法逐步落地为可运行的本地桌面 AI 会议助手。

---

## 一、项目起点：从比赛题目到真实业务场景

最初项目并不是直接进入编码，而是先围绕“比赛项目是否有真实业务价值”进行判断。用户提出：这不是单纯为了比赛，而是希望它具备真实企业落地潜力，尤其适合华科储能、华电集团这类国企/央企场景。

经过讨论，确定项目方向为：

> **AI 会议纪要 + 工作周报生成器**

选择这个方向的原因：

- 会议是企业高频工作流，痛点真实。
- 国企/央企会议内容敏感，不一定适合直接使用通用 SaaS 工具。
- 纪要、周报、领导汇报、项目台账都是体制内和能源企业高频材料。
- 项目既能展示 AI 能力，也能体现数据安全、本地部署、行业模板等业务价值。

项目定位从一开始就避免做成“简单转文字工具”，而是定义为：

> **面向高敏感会议场景的本地优先 AI 会议记录与纪要助手。**

---

## 二、产品需求阶段：先写 PRD，再进入开发

用户明确要求先写产品 PRD，不要直接编码。AI Coding 在这一阶段承担的是“产品经理 + 方案设计师”的角色。

### 2.1 形成的产品主线

产品被设计为一条完整的会议处理链路：

```text
录音采集
  -> 语音转文字
  -> 会议纪要生成
  -> 翻译
  -> 周报汇总
  -> 文档导出和历史归档
```

### 2.2 关键产品约束

在 PRD 阶段确定了几个重要原则：

- **本地优先**：音频尽量在本地采集、保存、处理。
- **云端可选**：高准确度 ASR 和 LLM 可接火山引擎/豆包。
- **不下载大模型作为第一阶段刚需**：本地 Ollama 仅预留接口。
- **中文目录和中文脚本命名**：方便展示和理解。
- **比赛但不低配**：按真实产品标准设计，而不是只做 Demo。

### 2.3 产出文档

AI Coding 生成并多轮更新：

- `产品需求文档_PRD.md`
- `技术方案设计.md`
- `开发排期计划.md`

后续随着项目技术路线变化，这三份文档又更新到最新版本：

- PRD 更新到 `V4.0`
- 技术方案更新到 `V3.0`
- 开发排期更新到 `V3.0`

---

## 三、技术方案阶段：确定本地桌面 + 云端增强架构

技术方案最初围绕 FastAPI、原生前端、Whisper、火山引擎、豆包 LLM 设计。

### 3.1 初始技术栈

| 模块 | 技术选择 | 原因 |
|------|----------|------|
| 后端 | Python FastAPI | 轻量、易调试、适合本地服务 |
| 前端 | HTML/CSS/Vanilla JS | 文件少、依赖低、便于展示 |
| 桌面 GUI | pywebview | 比浏览器打开更像正式桌面应用 |
| 原生录音 | sounddevice | 解决 macOS WebView 麦克风限制 |
| 实时 ASR | 火山引擎 WebSocket `bigmodel` | 支持边说边出文字 |
| 最终转写 | 火山云端 ASR + 分片兜底 | 比本地小模型更准确 |
| 纪要生成 | 火山方舟豆包 LLM | 用户已有可用模型能力 |
| 说话人标注 | 本地 Resemblyzer | 无需 HuggingFace Token，本地运行 |
| 本地扩展 | faster-whisper / Ollama 预留 | 作为后续离线增强路线 |

### 3.2 架构设计变化

项目并不是一次设计完成，而是在真实测试中不断调整。

最初设想：

```text
浏览器录音 -> 上传音频 -> 后端转写 -> LLM 生成纪要
```

后来调整为：

```text
pywebview 桌面应用
  -> Python 原生录音
  -> 实时 ASR 展示原文
  -> 停止后最终转写
  -> 本地声纹辅助标注
  -> LLM 生成纪要
```

最终形成当前比赛版主链路：

```text
会中：录音 + 波形 + 实时原文
会后：完整音频最终转写 + 发言人辅助标注 + 会议纪要生成
```

---

## 四、编码实现阶段：从项目骨架到核心功能

### 4.1 阶段 0：项目骨架

AI Coding 创建了项目基础目录：

```text
华科储能智能会议助手/
├── 启动程序.py
├── 桌面启动器.py
├── 依赖清单.txt
├── 后端服务/
├── 前端界面/
├── 配置与模板/
├── 测试脚本/
├── 录音缓存/
├── 模型文件/
└── 演示素材/
```

这一阶段完成：

- 一键启动脚本。
- FastAPI 后端骨架。
- 前端静态目录。
- 配置文件目录。
- 测试脚本目录。

### 4.2 阶段 1：语音转写引擎

最初实现三种 ASR 能力：

- 本地 faster-whisper 预留。
- 火山流式 ASR。
- 火山录音文件 ASR。

后来根据用户提供的火山文档，进一步调整协议细节。

关键文件：

- `后端服务/语音转写.py`
- `后端服务/语音转写_流式.py`
- `后端服务/语音转写_录音文件.py`
- `配置与模板/火山密钥_流式.py`
- `配置与模板/火山密钥_录音文件.py`

### 4.3 阶段 2：会议纪要生成

实现豆包 LLM 纪要生成能力。

内置四类模板：

- 部门例会。
- 项目评审。
- 客户沟通。
- 领导汇报。

关键文件：

- `后端服务/纪要生成.py`
- `配置与模板/Prompt配置.json`

这一阶段使项目从“转文字”升级为“生成会议成果”。

### 4.4 阶段 3：前端界面和录音能力

最初做浏览器页面，后续用户认为浏览器打开“不够高端”，因此改成桌面 GUI。

前端采用深色主题、华电蓝视觉风格、左右分屏布局：

- 左侧：录音、波形、实时原文、最终转写。
- 右侧：纪要模板、纪要生成、结果展示。

关键文件：

- `前端界面/界面.html`
- `前端界面/样式.css`
- `前端界面/交互.js`

---

## 五、关键踩坑与解决过程

这一部分是项目最有展示价值的内容，因为它体现了 AI Coding 不是一次性写代码，而是根据真实问题持续定位、修复和重构。

### 5.1 踩坑一：pywebview 中浏览器录音不可用

现象：

- 浏览器页面正常打开。
- 点击录音按钮没有弹窗。
- 没有生成录音。

原因分析：

- macOS 下 pywebview 使用 `WKWebView`。
- `WKWebView` 对浏览器 `getUserMedia` / 麦克风权限支持受限。

解决方案：

- 放弃依赖浏览器录音。
- 改为 Python 原生 `sounddevice` 录音。
- 通过 `pywebview.api` 把前端按钮桥接到 Python 后端。

结果：

- 桌面 GUI 仍然保留。
- 录音能力稳定可用。
- 更符合“本地优先”的产品定位。

### 5.2 踩坑二：火山 ASR WebSocket 协议 1006 异常

现象：

- 录音成功。
- 转写失败。
- 报错类似“云端语音识别连接关闭：无有效语音内容或音频过短”。
- WebSocket 出现 `code=1006`。

原因分析：

- 火山 ASR 二进制协议包格式要求严格。
- 早期在 Audio Only Request 中错误插入了 Sequence 字段。
- 服务端无法正确解析音频包。

修复方案：

- 按文档使用：

```text
Header + PayloadSize + Payload
```

- 移除错误 Sequence。
- 最后一包使用正确的结束标记。
- 正确处理服务端正常关闭 code。

结果：

- 云端 ASR 能正常返回转写文本。
- 基础录音转文字链路跑通。

### 5.3 踩坑三：WAV 容器和 PCM 数据格式混用

现象：

- 本地 WAV 文件存在。
- 云端识别仍然不稳定。

原因分析：

- 云端流式 ASR 更适合接收原始 PCM。
- 如果直接把完整 WAV 容器字节当音频流发送，可能导致识别异常。

修复方案：

- 对 WAV 文件剥离文件头。
- 发送 16kHz、16-bit、mono、little-endian PCM 数据。

结果：

- 识别稳定性提升。
- 文件转写和实时转写协议更清晰。

### 5.4 踩坑四：实时转写长录音丢字

现象：

- 录音 30 秒，只出十几个字。
- 录音 1 分半，大量文字丢失。
- 实时文本中途停止更新。

调试证据：

- 录音 chunk 仍在持续产生。
- ASR 发送队列出现积压。
- 火山返回 `waiting next packet timeout`。
- 说明录音链路还在，但实时 ASR 链路已经断掉。

原因分析：

- 实时 ASR WebSocket 长连接对发包节奏敏感。
- 同时做声纹分析可能占用计算资源。
- 额外 sleep、线程调度、队列积压都会导致实时链路超时。

调整方案：

- 去掉发送循环中不必要的额外 sleep。
- 录音中不再做实时声纹标注。
- 实时 ASR 只负责“会中原文预览”。
- 停止录音后再做最终转写和声纹整理。

最终架构：

```text
实时链路：快，但只作预览
最终链路：慢一点，但更完整、更可靠
```

### 5.5 踩坑五：波形太小、视觉效果差

现象：

- 用户反馈波形几乎看不到。
- 放大后仍然像噪声，不够美观。

原因分析：

- 麦克风输入采样点幅度本身较小。
- 早期用 RMS 柱状图，视觉冲击弱。

优化方案：

- 在 Python 端维护最近几秒音频环形缓冲。
- 前端获取真实波形采样点。
- 改成连续线条波形。
- 增加视觉增益、渐变、发光效果。

结果：

- 波形可见性显著提升。
- 桌面应用更像正式产品。

### 5.6 踩坑六：实时说话人分离不稳定

最初设想：

```text
录音中实时转写，同时每隔几秒做说话人标注
```

实际问题：

- 文本不流畅。
- 声纹标注消失。
- 同一个人被拆成不同发言人。
- 实时分析影响 ASR 稳定性。

调整结果：

- 放弃强追求“实时准确分人”。
- 改为“会后基于完整音频做辅助标注”。

这体现了一个重要产品判断：

> 比起现场实时炫技，更重要的是录音不丢、最终文本完整、纪要可用。

### 5.7 踩坑七：Resemblyzer 多人识别能力有限

用户测试发现：

- 多人语音基本只标出 A/B。
- 3 人以上分不清楚。

调试检测：

- 后端内部 diarization 确实只输出 2 个 speaker。
- 不是前端显示问题。
- 自动估计人数时，`n=2` 的轮廓系数最高。
- 强制 3/4 人可以分出来，但质量更低、更乱。

结论：

> 当前 Resemblyzer + 聚类方案主要适合辅助标注，不适合承诺多人高精度说话人分离。

产品调整：

- 不再把它宣传为“精准多人识别”。
- 改为“本地声纹辅助标注”。
- 下一步通过“发言人重命名/人工校正”提升可用性。

### 5.8 踩坑八：最终长音频转写失败

现象：

- 停止录音后，最终转写阶段失败。
- 出现 `code=1006`。

原因分析：

- 整段较长音频直接走流式 WebSocket 不稳定。
- 火山录音文件接口理论更适合长音频，但需要公网 URL 或对象存储。

比赛版解决方案：

- 使用大段分片兜底。
- 初始尝试 25 秒，但用户认为太短，影响上下文。
- 最终调整为默认约 `45s`。
- 如果单段失败，再降级为约 `22.5s`。

产品边界：

- 这是比赛演示的稳定方案。
- 企业级最终方案应接录音文件识别、TOS/内网对象存储、私有化 ASR 或 WhisperX。

---

## 六、重要架构收敛：从“功能堆叠”到“稳定会议系统”

项目中后期，用户提出一个关键问题：

> 会议系统正常可能使用两小时以上，不能只做比赛 Demo。

围绕这个问题，项目架构发生了重要收敛。

### 6.1 之前的问题

早期方案过于追求同时完成：

- 实时录音。
- 实时转写。
- 实时说话人分离。
- 实时展示标注文本。

结果是系统复杂度升高，长录音稳定性变差。

### 6.2 收敛后的原则

最终确立：

```text
录音主链路绝对优先
实时原文只作预览
最终文本会后整理
声纹标注辅助使用
纪要生成基于最终文本
```

### 6.3 当前 MVP 架构

```text
pywebview 桌面应用
  -> sounddevice 原生录音
  -> 实时 ASR 原文展示
  -> 停止后上传完整 WAV
  -> 45 秒大段分片最终转写
  -> 完整音频本地声纹辅助标注
  -> 合并为最终文本
  -> 豆包 LLM 生成会议纪要
```

### 6.4 企业级演进路线

文档中明确了企业级落地路线：

```text
完整音频保存
  -> 会中实时预览
  -> 会后长音频批处理转写
  -> 全局说话人聚类
  -> 时间戳对齐
  -> 人工校正
  -> LLM 生成纪要/周报/台账
```

这一点对答辩很重要，因为它说明项目不是盲目堆 Demo，而是知道当前边界和后续落地路线。

---

## 七、AI Coding 在项目中的具体作用

### 7.1 产品经理角色

AI Coding 帮助完成：

- 从比赛题目抽象出业务场景。
- 识别国企/央企数据安全约束。
- 定义本地优先产品定位。
- 梳理用户画像、使用场景、功能边界。
- 输出 PRD、技术方案、排期计划。

### 7.2 架构师角色

AI Coding 帮助完成：

- 设计 FastAPI + pywebview + 原生前端架构。
- 设计三类 ASR 引擎接口。
- 设计实时原文和最终整理两阶段架构。
- 评估 Resemblyzer、pyannote、WhisperX 等方案边界。
- 规划企业级演进路线。

### 7.3 程序员角色

AI Coding 生成和修改：

- 后端服务代码。
- 前端界面代码。
- 桌面启动器代码。
- 原生录音代码。
- 语音转写协议代码。
- LLM 纪要生成代码。
- 说话人分离代码。
- 测试脚本和调试脚本。

### 7.4 调试工程师角色

AI Coding 在问题出现后没有直接“猜修复”，而是通过：

- 提出假设。
- 增加日志。
- 收集运行时证据。
- 分析队列、WebSocket、文本长度、音频 chunk。
- 对比修复前后结果。

典型证据包括：

- 实时 ASR 出现 `waiting next packet timeout`。
- 长录音只返回部分文本。
- diarization 内部只输出 2 个 speaker。
- silhouette 分数显示 `n=2` 最优。

### 7.5 技术文档助手角色

AI Coding 持续维护：

- PRD。
- 技术方案。
- 开发排期。
- 调试记录。
- 当前这份全过程记录。

这使项目不只是有代码，还能向评委解释“为什么这么做、怎么一步步做出来”。

---

## 八、当前项目状态

### 8.1 已经具备的能力

```text
桌面应用启动
  -> 原生录音
  -> 波形展示
  -> 实时原文
  -> 停止后最终转写
  -> 发言人辅助标注
  -> 会议纪要生成
```

### 8.2 当前能力边界

- 实时原文可以展示，但不承诺现场文本完全准确。
- 说话人标注是辅助能力，不承诺多人精准识别。
- 45 秒分片是比赛版兜底，不是企业级最终长会议方案。
- 企业级落地需要进一步接入长音频文件识别、私有化 ASR 或专业 diarization。

### 8.3 当前最适合的展示话术

不建议说：

> 我们做了一个会议转文字工具。

建议说：

> 我们做的是面向高敏感会议场景的本地优先 AI 会议治理工具。它通过会中实时原文、会后最终整理、声纹辅助标注和大模型纪要生成，把会议音频转化为可编辑、可归档、可汇报的知识资产。

---

## 九、下一步开发规划

根据最新排期，下一步不建议继续死磕当前 Resemblyzer 的多人识别，而是做更能提升比赛观感和产品完整度的功能。

### 9.1 第一优先级：发言人重命名/校正

目标：

```text
[发言人 A] -> [张经理]
[发言人 B] -> [李工]
```

价值：

- 弥补本地模型多人识别能力不足。
- 更符合真实会议纪要整理习惯。
- 让纪要生成结果更专业。
- 比赛演示观感提升明显。

### 9.2 第二优先级：历史记录 + Word 导出

目标：

- 保存会议录音路径。
- 保存实时文本。
- 保存最终文本。
- 保存纪要。
- 支持导出 `.docx`。

价值：

- 形成会议闭环。
- 适合国企/央企办公场景。
- 方便归档和汇报。

### 9.3 第三优先级：周报汇总

目标：

- 多条会议纪要自动聚合。
- 生成部门周报、项目周报。

价值：

- 业务价值强。
- 和用户最初选题“会议纪要 + 工作周报”高度一致。
- 更能体现 AI 对管理工作的帮助。

### 9.4 第四优先级：翻译功能

目标：

- 中文纪要转英文。
- 英文材料转中文。
- 储能行业术语保护。

适用场景：

- 海外项目。
- 客户沟通。
- 技术交流。

### 9.5 演示前必须做

- 清理临时 debug 埋点。
- 准备 1-2 分钟双人会议演示素材。
- 准备备用 Mock 文本。
- 准备 5 分钟讲解脚本。
- 明确说明“辅助标注”和“企业级增强路线”。

---

## 十、用于 PPT 的项目开发叙事

可以在 PPT 中按以下结构讲：

### 第 1 页：项目背景

国企/央企会议多、内容敏感、纪要整理耗时，通用 SaaS 工具存在数据安全和采购适配问题。

### 第 2 页：项目定位

本地优先 AI 会议助手：录音、实时原文、会后整理、声纹辅助、纪要生成、周报汇总。

### 第 3 页：AI Coding 开发方式

通过自然语言对话完成：

- 需求分析。
- PRD 编写。
- 技术方案设计。
- 项目骨架生成。
- 代码实现。
- 真实测试。
- Bug 调试。
- 架构重构。
- 文档同步。

### 第 4 页：开发路线

```text
选题 -> PRD -> 技术方案 -> 项目骨架 -> ASR -> LLM 纪要
-> 桌面 GUI -> 原生录音 -> 实时转写 -> 声纹标注
-> 长录音稳定性优化 -> 产品化收敛
```

### 第 5 页：关键踩坑

- pywebview 麦克风不可用。
- 火山 ASR 协议 1006。
- WAV/PCM 格式问题。
- 长录音实时转写丢字。
- 波形太小。
- 实时声纹影响稳定性。
- Resemblyzer 多人识别能力有限。

### 第 6 页：关键优化

- 浏览器录音改为 Python 原生录音。
- 实时分人改为会后整理。
- 最终转写增加 45 秒大段分片兜底。
- UI 拆成实时原文和最终整理两个文本区。
- 说话人标注定位为辅助能力。
- 文档明确企业级演进路线。

### 第 7 页：当前成果

可现场演示：

- 启动桌面应用。
- 录音。
- 实时原文。
- 停止后最终转写。
- 发言人辅助标注。
- 一键生成会议纪要。

### 第 8 页：后续规划

- 发言人重命名。
- Word 导出。
- 历史记录。
- 周报汇总。
- 翻译。
- 企业级长会议处理。

---

## 十一、给新对话使用的上下文摘要

如果后续开启新对话，可以直接让 AI 读取本文件，并重点理解以下结论：

1. 项目是“华科储能智能会议助手”，不是普通转文字工具。
2. 当前采用 pywebview 桌面 GUI + FastAPI + 原生 HTML/CSS/JS。
3. macOS WebView 麦克风不可用，因此使用 `sounddevice` 原生录音。
4. 火山 ASR 负责实时原文和最终转写，豆包 LLM 负责纪要生成。
5. 当前最终转写采用约 `45s` 分片，失败降级到约 `22.5s`。
6. 说话人标注使用本地 Resemblyzer，是辅助能力，不适合多人高精度识别。
7. 当前产品策略是“会中实时原文 + 会后最终整理”。
8. 下一步优先做“发言人重命名/校正”，然后做历史记录、Word 导出、周报汇总、翻译。
9. 企业级路线是完整音频保存、长音频识别接口、全局时间轴、专业 diarization、人工校正闭环。
10. 所有文档已同步更新：PRD V4.0、技术方案 V3.0、开发排期 V3.0。

---

## 十二、总结

这个项目的价值不只在于最终代码，而在于完整展示了 AI Coding 的协同开发流程：

- 用户提出业务判断和真实测试反馈。
- AI 辅助完成产品设计、技术设计、代码实现和调试分析。
- 项目在真实问题中不断调整，而不是一次性生成静态 Demo。
- 最终形成了一个能演示、能解释、能继续演进的本地 AI 会议助手。

最重要的经验是：

> AI Coding 的价值不是替代人做决定，而是把人的业务判断快速转化为产品文档、技术方案、可运行代码和可验证结果，并在真实反馈中持续迭代。

